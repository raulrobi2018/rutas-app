import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  centerIcon: {
    position: 'absolute',
    bottom: 20,
    right: 20,
  },

  polylineIcon: {
    position: 'absolute',
    bottom: 20,
    right: 100,
  },
});
